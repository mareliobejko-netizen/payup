import Link from "next/link";
import { ArrowLeft, KeyRound, ShieldCheck } from "lucide-react";

export default async function ForgotPasswordPage({searchParams}:{searchParams:Promise<{error?:string}>}){
  const p=await searchParams;
  return <main className="min-h-screen bg-zinc-950 px-5 py-8 text-white"><div className="mx-auto max-w-md"><Link href="/login" className="flex h-11 w-11 items-center justify-center rounded-full bg-zinc-900"><ArrowLeft size={20}/></Link><div className="mt-10 flex h-16 w-16 items-center justify-center rounded-3xl bg-lime-400 text-black"><KeyRound size={28}/></div><h1 className="mt-5 text-3xl font-black">Recupera password</h1><p className="mt-2 text-sm leading-6 text-zinc-500">Inserisci username/email e il codice di recupero che hai generato nel Profilo.</p>{p.error&&<div className="mt-5 rounded-2xl bg-red-500/10 p-4 text-sm font-bold text-red-300">{p.error}</div>}<form action="/reset-password" method="get" className="mt-6 space-y-4"><input name="identifier" required placeholder="Username o email" className="h-14 w-full rounded-2xl border border-white/10 bg-zinc-900 px-4 outline-none focus:border-lime-400"/><input name="code" required placeholder="Codice di recupero" className="h-14 w-full rounded-2xl border border-white/10 bg-zinc-900 px-4 font-mono uppercase outline-none focus:border-lime-400"/><button className="h-14 w-full rounded-2xl bg-lime-400 font-black text-black">CONTINUA</button></form><div className="mt-5 flex gap-2 rounded-2xl bg-zinc-900 p-4 text-xs leading-5 text-zinc-500"><ShieldCheck size={18} className="shrink-0 text-lime-400"/>Il codice di recupero è personale. Se non ne hai mai generato uno, dovrai accedere al tuo account prima di poterlo configurare.</div></div></main>
}
