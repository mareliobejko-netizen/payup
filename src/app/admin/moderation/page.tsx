import Link from "next/link";
import { ArrowLeft, Eye, EyeOff, Flag, ShieldCheck } from "lucide-react";
import { desc, eq, sql } from "drizzle-orm";
import { db } from "@/db";
import { penalties, proofReports, proofs, users } from "@/db/schema";
import { requirePayUpAdmin } from "@/lib/auth";
import ProofMedia from "@/components/proof-media";
import { dismissReportsAction, hideProofAction, restoreProofAction } from "./actions";
export const dynamic='force-dynamic';
export default async function ModerationPage({searchParams}:{searchParams:Promise<{success?:string}>}){await requirePayUpAdmin();const {success}=await searchParams;const rows=await db.select({proofId:proofs.id,mediaUrl:proofs.mediaUrl,mediaType:proofs.mediaType,isHidden:proofs.isHidden,title:penalties.title,username:users.username,reports:sql<number>`count(${proofReports.id})`,latest:sql<Date>`max(${proofReports.createdAt})`}).from(proofReports).innerJoin(proofs,eq(proofReports.proofId,proofs.id)).innerJoin(penalties,eq(proofs.penaltyId,penalties.id)).innerJoin(users,eq(proofs.uploadedBy,users.id)).where(eq(proofReports.status,'open')).groupBy(proofs.id,penalties.title,users.username).orderBy(desc(sql`max(${proofReports.createdAt})`));return <main className="min-h-screen bg-zinc-950 text-white"><div className="mx-auto max-w-md p-5 pb-12"><div className="flex items-center gap-3"><Link href="/profile" className="flex h-11 w-11 items-center justify-center rounded-full bg-zinc-900"><ArrowLeft size={20}/></Link><div><p className="text-xs font-black uppercase tracking-[.25em] text-red-300">PayUp Admin</p><h1 className="text-xl font-black">Moderazione The Wall</h1></div></div>{success&&<div className="mt-5 rounded-2xl bg-lime-400/10 p-3 text-sm font-bold text-lime-300">✓ {success}</div>}<section className="mt-6 rounded-3xl border border-white/10 bg-zinc-900 p-4"><div className="flex items-center gap-2"><ShieldCheck className="text-red-300" size={19}/><p className="font-black">Segnalazioni aperte</p></div><p className="mt-1 text-xs text-zinc-500">Nascondere un post lo rimuove da The Wall e dai link pubblici, senza cancellare la prova dal gruppo.</p></section><div className="mt-5 space-y-5">{rows.length===0?<div className="rounded-3xl border border-dashed border-white/10 p-10 text-center"><Flag className="mx-auto text-zinc-600"/><p className="mt-3 font-black">Nessuna segnalazione aperta</p><p className="mt-1 text-sm text-zinc-500">The Wall è tranquillo.</p></div>:rows.map(r=><article key={r.proofId} className="overflow-hidden rounded-3xl border border-white/10 bg-zinc-900"><div className="p-4"><div className="flex items-center justify-between gap-2"><div><p className="font-black">@{r.username}</p><p className="text-xs text-zinc-500">{r.title}</p></div><span className="rounded-full bg-red-500/10 px-3 py-1 text-xs font-black text-red-300">🚩 {Number(r.reports)}</span></div></div><ProofMedia src={r.mediaUrl} type={r.mediaType} alt={r.title}/><div className="grid grid-cols-2 gap-2 p-4">{r.isHidden?<form action={restoreProofAction}><input type="hidden" name="proofId" value={r.proofId}/><button className="flex h-11 w-full items-center justify-center gap-2 rounded-2xl bg-lime-400/10 text-xs font-black text-lime-300"><Eye size={16}/>RIPRISTINA</button></form>:<form action={hideProofAction}><input type="hidden" name="proofId" value={r.proofId}/><button className="flex h-11 w-full items-center justify-center gap-2 rounded-2xl bg-red-500/10 text-xs font-black text-red-300"><EyeOff size={16}/>NASCONDI</button></form>}<form action={dismissReportsAction}><input type="hidden" name="proofId" value={r.proofId}/><button className="h-11 w-full rounded-2xl bg-zinc-800 text-xs font-black">ARCHIVIA</button></form></div></article>)}</div></div></main>}
