"use client";
import { Flag, X } from "lucide-react";import { useState } from "react";import { reportProofAction } from "@/app/report/actions";
export default function ReportProof({proofId,back}:{proofId:string;back:string}){const [open,setOpen]=useState(false);return <>{<button onClick={()=>setOpen(true)} className="flex items-center gap-2 rounded-full bg-zinc-800 px-3 py-2 text-xs font-black text-zinc-400"><Flag size={15}/> Segnala</button>}{open&&<div className="fixed inset-0 z-50 flex items-end justify-center bg-black/75 p-4 sm:items-center"><div className="w-full max-w-md rounded-3xl border border-white/10 bg-zinc-950 p-5"><div className="flex items-center justify-between"><h3 className="text-lg font-black">Segnala contenuto</h3><button onClick={()=>setOpen(false)} className="h-9 w-9 rounded-full bg-zinc-800"><X className="mx-auto" size={17}/></button></div><p className="mt-2 text-sm text-zinc-500">La segnalazione non cancella automaticamente il post.</p><form action={reportProofAction} className="mt-5 space-y-3"><input type="hidden" name="proofId" value={proofId}/><input type="hidden" name="back" value={back}/><select name="reason" className="h-12 w-full rounded-2xl border border-white/10 bg-zinc-900 px-3"><option value="spam">Spam</option><option value="offensive">Contenuto offensivo</option><option value="privacy">Problema di privacy</option><option value="dangerous">Contenuto pericoloso</option><option value="other">Altro</option></select><textarea name="note" maxLength={500} rows={3} placeholder="Nota facoltativa" className="w-full rounded-2xl border border-white/10 bg-zinc-900 p-3"/><button className="h-12 w-full rounded-2xl bg-red-500/15 font-black text-red-300">INVIA SEGNALAZIONE</button></form></div></div>}</>}
